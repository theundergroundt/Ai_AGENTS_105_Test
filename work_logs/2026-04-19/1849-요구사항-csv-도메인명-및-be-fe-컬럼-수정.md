# 작업 로그

## 시간

2026-04-19 18:49

## 사용자 요청

- 요구사항 명세서의 도메인을 `아키텍처`, `오케스트레이션`, `인증`, `세션`, `설정` 같은 문서형 용어로 바꿔 달라는 요청
- `담당` 컬럼 대신 `be`, `fe` 컬럼을 분리하고, 해당되면 `1`을 넣는 방식으로 바꿔 달라는 요청

## 답변/작업 요약

- 요구사항 CSV의 헤더를 `ID, 대상, 도메인, be, fe, 우선순위, 제목, 요구사항, 근거, 단계`로 변경했다.
- 기존 `담당` 값이 `BOTH`, `BE`, `FE`였던 항목들을 각각 `be=1`, `fe=1` 방식으로 재구성했다.
- 도메인명은 더 설계 문서형에 가까운 용어로 바꿨다.
  - 예: `런 -> 실행`, `요청입력 -> 입력`, `도구실행 -> 도구`, `단계추적 -> 모니터링`, `이벤트스트림 -> 이벤트`, `상태투영 -> 상태`, `시각화 -> UI`, `운영관리 -> 운영`, `확장성 -> 아키텍처/확장`, `범위제외 -> 범위`

## 변경 사항

- `analysis/projects/spring-agent-runtime-requirements-spec.csv`
  - `담당` 컬럼 제거
  - `be`, `fe` 컬럼 추가
  - 도메인명 문서형 용어로 변경
- `work_logs/2026-04-19/1849-요구사항-csv-도메인명-및-be-fe-컬럼-수정.md`
  - 현재 작업 로그 신규 작성

## 관련 파일

- `Ai_AGENTS_105_Test/analysis/projects/spring-agent-runtime-requirements-spec.csv`

## 결정 또는 해석

- 요구사항 CSV는 이후 구현 분담과 API 연결을 위해 `담당자 문자열`보다 `be/fe 플래그` 형태가 더 실용적이다.
- 도메인명은 기능 설명형보다 아키텍처/설계 문서형 용어가 이후 명세 문서와 연결하기 쉽다.

## 다음 단계

- 같은 형식으로 API 명세서 CSV도 `도메인 + be/fe` 기준으로 작성
