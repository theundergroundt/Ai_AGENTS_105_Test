# Forks 작업 지침

이 지침은 `forks`와 그 하위 폴더에 적용된다.

## 목적

- 외부 오픈소스 저장소를 분리된 checkout으로 유지한다.

## 규칙

- 저장소별 폴더 하나에 코드베이스 하나만 둔다.
- 분석 결과는 가능하면 저장소 안이 아니라 `../analysis`에 적는다.
- 외부 저장소를 수정해야 하는 작업이 아니면 코드 변경을 기본값으로 하지 않는다.
- 원격 저장소 정보와 branch 정보는 보존하되 개인 비밀값은 커밋하지 않는다.
