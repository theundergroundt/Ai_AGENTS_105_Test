# 프로젝트 작업 지침

이 지침은 `projects/hermes-like-agent`와 그 하위 폴더에 적용된다.

## 목적

- 자체 개발 AI agent 프로젝트의 설계와 구현 기반을 정리한다.

## 규칙

- 외부 소스의 분석 메모는 가능하면 `../../analysis`에 먼저 쓴다.
- 프로젝트에 직접 필요한 해석, 결정, TODO만 여기로 가져온다.
- 결정이 생기면 `decisions`에 남긴다.
- 불확실한 아이디어는 `notes`에 두고 확정된 내용만 `docs`로 올린다.
